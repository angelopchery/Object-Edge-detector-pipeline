# Original photos never leave this machine via git
data/raw/

# Dataset images are distributed via download_data.py, not git.
# Label files and the split manifest ARE committed (annotation is auditable work).
data/dataset/images/
data/*.zip

# Training outputs
runs/

# Model weights (>50 MB checkpoints don't belong in git; use a release/Drive link)
*.pt
*.onnx

# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# Virtual environments
artikate/
venv/
.venv/
env/

# OS / editor noise
Thumbs.db
.DS_Store
.vscode/
.idea/
